version: '2'
services:
  db:
    image: postgres
  web:
    image: django:v1.1
    build: 
        context: .
        dockerfile: Dockerfile.python
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
